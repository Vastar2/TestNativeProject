import React from 'react';
import {Text, View} from 'react-native';
import {styles} from '../styles/styles';

const Footer = () => {
  return (
    <View style={styles.footer}>
      <Text>My first mobile app. You can admire this masterpiece</Text>
    </View>
  );
};

export default Footer;
